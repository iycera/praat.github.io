# Praat 国际化（i18n）说明

Praat 支持多语言界面，方便切换和扩展语言。本文档介绍 i18n 文件结构、添加新语言/翻译、切换方法和相关脚本。

## 系统特性

Praat 的 i18n 系统采用**内置与外挂相结合**的方式：
- **内置语言包**：编译时嵌入到可执行文件中，无需外部文件即可运行
- **外挂语言包**：放置在 `exe目录/praat_i18n` 文件夹中，可随时添加或更新
- **优先级**：内置语言包优先，外挂语言包作为补充
- **灵活性**：内置和外挂可以包含相同的语言代码，用户可以选择使用哪个版本

## 界面与多语言菜单效果

主界面多语言菜单示例：

![主窗口多语言菜单](./docs/pictures/mainwindow.png)

编辑窗口中文界面示例：

![编辑器中文界面](./docs/pictures/view&editor.png)

---

## 1. 目录结构及文件说明

### 内置语言包（编译时嵌入）

```
sys/language_packs/
├─ en-US.json          # 英文包（内置）
├─ zh-CN.json          # 简体中文包（内置）
├─ ko-KR.json          # 韩语包（内置）
├─ ja-JP.json          # 日语包（内置）
├─ ...                 # 更多内置语言
├─ languages.json      # 内置语言列表与映射
├─ batch_translate_de.py          # 德语批量翻译示例脚本
├─ comprehensive_translate_de.py  # 德语自动翻译示例
├─ compare_translations.py        # key 检查/翻译完整性脚本
```

内置语言包会被编译进可执行文件，确保即使没有外部文件也能正常运行。

### 外挂语言包（运行时加载）

```
exe目录/praat_i18n/
├─ languages.json      # 外挂语言列表与映射（可选）
├─ en-US.json          # 外挂英文包（可覆盖或补充内置）
├─ zh-CN.json          # 外挂中文包
├─ ...                 # 更多外挂语言
```

外挂语言包路径：
- **Windows**: `可执行文件所在目录\praat_i18n`
- **Linux/Unix**: `可执行文件所在目录/praat_i18n`
- **macOS**: `可执行文件所在目录/praat_i18n`（或当前目录）

### 示例：languages.json 配置
```json
{
  "languages": [
    { "code": "en-US", "name": "English", "file": "en-US.json" },
    { "code": "zh-CN", "name": "简体中文", "file": "zh-CN.json" },
    { "code": "ja-JP", "name": "日本語", "file": "ja-JP.json" },
    ...
  ],
  "default": "en-US"
}
```

### 示例：zh-CN.json 片段
```json
{
  "menu.praat": "Praat",
  "menu.new": "新建",
  "menu.open": "打开",
  "menu.save": "保存",
  "menu.i18n": "国际化",
  ...
}
```

---
## 2. 如何添加新语言和翻译

### 添加内置语言包

1. **拷贝已有 JSON** 将 `sys/language_packs/en-US.json` 复制为 `sys/language_packs/es-ES.json` 并翻译内容
2. **注册新语言** 编辑 `sys/language_packs/languages.json` 添加：
```json
{ "code": "es-ES", "name": "Español", "file": "es-ES.json" }
```
3. **补全全部 key** 建议用 `compare_translations.py` 检查完整性
4. **重新编译** 运行编译脚本时，会自动生成嵌入语言包并编译进可执行文件
   - 无需手动运行 `generate_embedded_packs.py`（编译脚本会自动执行）

### 添加外挂语言包

1. **创建目录** 在可执行文件同目录下创建 `praat_i18n` 文件夹（如果不存在）
2. **拷贝 JSON 文件** 将语言包 JSON 文件（如 `es-ES.json`）放入 `praat_i18n` 目录
3. **可选：注册语言** 在 `praat_i18n/languages.json` 中添加语言注册信息（如果不存在该文件，系统会自动扫描目录）
4. **即时生效** 外挂语言包无需重新编译，重启程序即可在菜单中看到

### 语言包格式

内置和外挂语言包使用相同的 JSON 格式，可互相兼容。

---
## 3. 语言切换方法

### 菜单结构

i18n 菜单包含以下结构（菜单项名称已国际化，会根据当前语言显示对应翻译）：
```
i18n
├─ 内置          # 内置语言子菜单（国际化名称，如 "Built-in"、"内置" 等）
│  ├─ English         # 内置语言选项（带勾选标记）
│  ├─ 简体中文
│  └─ ...
├─ 外挂          # 外挂语言子菜单（国际化名称，如 "External"、"外挂" 等）
│  ├─ English         # 外挂语言选项（如果存在）
│  ├─ 简体中文
│  └─ ...
└─ 简体中文 (内置)       # 当前使用的语言（显示名称和来源标记，已国际化）
```

**注意**：子菜单名称（"内置"、"外挂"）和当前语言标记（"(内置)"、"(外挂)"）会根据当前选择的界面语言自动显示对应翻译。例如：
- 英文界面：显示 "Built-in"、"External"、"(Built-in)"
- 中文界面：显示 "内置"、"外挂"、"(内置)"
- 其他语言：显示对应的翻译

### 切换步骤

1. **选择语言**：在菜单"i18n"下选择"内置"或"外挂"子菜单中的语言（子菜单名称已国际化）
2. **自动保存**：选择会保存到 `praat_language_preference.txt`，记录语言代码和来源（内置/外挂）
3. **重启生效**：勾选语言后，需重启程序方能生效
4. **当前语言显示**：菜单底部显示当前使用的语言名称及来源标记（已国际化，如 "简体中文 (内置)" 或 "English (Built-in)"）

### 语言选择优先级

- 系统优先加载内置语言包
- 如果内置语言包不存在，则尝试加载外挂语言包
- 如果都不可用，回退到英文（最小化版本）

---
## 4. 编译与辅助脚本

### 编译脚本

- **`compile_praat.bat`**：全量编译脚本
  - 自动检测 MinGW 环境
  - **自动生成嵌入语言包**（每次编译前运行 `generate_embedded_packs.py`）
  - 使用 32 位编译（默认，避免 UTF-8 乱码问题）
  - 多线程编译（4 线程）
  - 编译后罗列语言包信息和建议
  - 如需 64 位编译，修改脚本中的 `msys-mingw32` 为 `msys-mingw64`

- **`compile_quick.bat`**：快速编译脚本
  - 关闭 Praat 进程后快速编译
  - 同样会自动生成嵌入语言包
  - 默认使用 32 位编译

- **`clean_praat.bat`**：清理脚本
  - 清理编译产物（.o/.a 文件）
  - 清理调试文件和语言偏好文件

### 嵌入语言包生成

**`sys/generate_embedded_packs.py`**：自动生成嵌入语言包脚本
- **作用**：将 `sys/language_packs/*.json` 中的所有语言包编译到 `sys/i18n_embedded_packs.cpp` 中
- **自动执行**：每次运行编译脚本时会自动执行，无需手动运行
- **手动执行**：如需单独更新嵌入语言包，可运行：
  ```bash
  python sys/generate_embedded_packs.py
  ```
- **编码处理**：自动处理 UTF-8 BOM，确保多语言字符正确嵌入

### 编译说明

- **32 位 vs 64 位**：
  - 默认使用 32 位编译（`makefile.defs.msys-mingw32`），可更好地避免 UTF-8 字符显示乱码
  - 如需 64 位编译，修改编译脚本中的配置即可
- **嵌入语言包**：
  - 编译时会自动从 JSON 文件生成嵌入语言包
  - 确保每次编译都包含最新的语言包内容
  - 生成的 `i18n_embedded_packs.cpp` 文件较大（约 1.7MB），这是正常现象

---
## 5. 批量翻译与辅助 Python 脚本
- `batch_translate_de.py`：德语批量机翻示例，可仿造扩展其他目标语言。
- `comprehensive_translate_de.py`：德语全量自动填充（按映射规则或可扩展用云翻译API）。
- `compare_translations.py` 用法示例：
  
  ```bash
  python compare_translations.py en-US.json zh-CN.json 中文
  ```
  自动输出未翻译、部分翻译 key 和建议。

---
## 6. 典型 key 中英对照
```json
// en-US.json
{
  "menu.praat": "Praat",
  "menu.save": "Save",
  "menu.language": "Language",
  "label.confirm": "Are you sure?",
  ...
}
// zh-CN.json
{
  "menu.praat": "Praat",
  "menu.save": "保存",
  "menu.language": "语言",
  "label.confirm": "您确定吗？",
  ...
}
```

---
## 7. 测试/调试建议

### 语言包测试
- 建议生成新包用 `compare_translations.py` 对齐 key
- 本地切换多语言，完整遍历界面确保无英文残留
- 测试内置和外挂语言包的加载顺序和优先级

### 自包含测试
- 将可执行文件复制到新目录（不包含 `praat_i18n` 文件夹）
- 验证内置语言包是否正常工作
- 验证外挂语言包路径是否正确识别

### 调试信息
- 调试日志在 `debug_i18n_simple.txt`
- 语言偏好保存在 `praat_language_preference.txt`（格式：`语言代码:内置/外挂`）

---
## 8. 贡献指南
欢迎通过 PR 贡献语言包，需：
- 新增 json，注册进 languages.json
- 尽量 key 完整，附简要说明
- 可附运行截图

更详细批量翻译/脚本/最佳实践欢迎参与讨论！

## 各语言包翻译进度说明

主流语言包自动 key 数统计（以 en-US 为母包）：

| 语言名         | key数量 | 覆盖比例   | 备注             |
|------------|--------|--------|------------------|
| English    | 1999   | 100%   | 母包             |
| 简体中文      | 1999   | 99.9%+ | 全结构已全量汉化      |
| 其他主流语种    | 1800~1990 | ≥90%   | 少量新菜单/help待补充 |
| Qǔei/lk-CN等 | 1~1000 | DEMO  | 测试/部分功能演示性包   |

> 说明：建议定期用 compare_translations.py 检测 key 完整性，覆盖率基本可保持99%以上。
